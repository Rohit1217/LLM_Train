# Core training stack (versions observed in the working env; adjust CUDA build as needed)
torch==2.9.1
triton                    # ships with torch; pin to your installed version if needling reproducibility
liger-kernel             # fused linear cross-entropy (LigerFusedLinearCrossEntropyLoss)

# tokenizer / data
tiktoken                 # tokenizer_fast backend
numpy
datasets==4.8.4          # eval.py: MMLU / GSM8K loading

# logging / utils
wandb==0.19.11
tqdm
